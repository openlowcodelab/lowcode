通过 `sudo docker-compose up -d` 启动 Redis/RabbitMQ 等依赖；使用 Tools 下各服务的 DbMigrator 执行数据库迁移（如 `dotnet run --project src/Tools/H.Account.DbMigrator`）；主程序通过 `dotnet run --project src/Host/H.AppLab.Host.All/H.AppLab.Host.All --launch-profile HostAll` 启动，默认访问 http://localhost:5045。