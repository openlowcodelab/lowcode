- 各服务模块遵循 Application.Contracts / Application / EntityFrameworkCore / Web 四层分层结构
- 前端通过 IAppService 接口 + H.Abp.HttpClientProxy 动态代理调用 HTTP，服务端进程内实现与客户端 HTTP 代理共用同一套接口
- 宿主项目统一采用 Blazor Web App 模式（Server + WebAssembly Client），业务逻辑仅做服务注册不包含具体实现