@namespace H.LowCode.PartsDesignEngine
@inherits LowCodeComponentBase

@if (Component.IsContainer)
{
    <span style="font-size:10px; color:gray; margin-left:10px;">组件标识：@Component.PartsId</span>
}
else
{
    <div class="propertysetting-item">
        <label>Name<span style="font-size:10px; color:gray;"> (字段名称/英文)</span>：</label>
        <label style="float:right; color:gray;">@Component.PartsId</label>
        <input class="inp" @bind="Component.Name" @bind:after="() => OnPropertyChange()" />
    </div>
    <div class="propertysetting-item">
        <label>标题：</label>
        <input class="inp" @bind="Component.Label" @bind:after="() => OnPropertyChange()" />
    </div>
    <div class="propertysetting-item">
        <label>描述：</label>
        <textarea class="ta" @bind="Component.Description" @bind:after="() => OnPropertyChange()"></textarea>
    </div>
}

@code {
    [Parameter]
    public ComponentPartsSchema Component { get; set; }

    protected override async Task OnInitializedAsync()
    {
        await base.OnInitializedAsync();
    }

    private void OnPropertyChange()
    {
        Component.RefreshState();
    }
}
