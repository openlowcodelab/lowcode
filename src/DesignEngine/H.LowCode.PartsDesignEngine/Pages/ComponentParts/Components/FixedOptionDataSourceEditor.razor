@namespace H.LowCode.PartsDesignEngine
@inherits LowCodeComponentBase

<div class="fixed-option-data-source-editor">
    @if (OptionsList != null)
    {
        @for (int i = 0; i < OptionsList.Count; i++)
        {
            var option = OptionsList[i];
            <AntDesign.Card Size="@CardSize.Small" Style="margin-bottom: 8px;">
                <AntDesign.ChildContent>
                    <AntDesign.Row Gutter="16">
                        <AntDesign.Col Span="8">
                            <div style="margin-bottom: 16px;">
                                <label style="display: block; margin-bottom: 4px; font-weight: 500;">显示文本</label>
                                <Input @bind-Value="@option.Label" Placeholder="显示文本" />
                            </div>
                        </AntDesign.Col>
                        <AntDesign.Col Span="8">
                            <div style="margin-bottom: 16px;">
                                <label style="display: block; margin-bottom: 4px; font-weight: 500;">选项值</label>
                                <Input @bind-Value="@option.Value" Placeholder="选项值" />
                            </div>
                        </AntDesign.Col>
                        <AntDesign.Col Span="8">
                            <div style="margin-bottom: 16px;">
                                <label style="display: block; margin-bottom: 4px; font-weight: 500;">是否选中</label>
                                <Switch @bind-Checked="@option.IsSelected" />
                            </div>
                        </AntDesign.Col>
                    </AntDesign.Row>
                    <AntDesign.Row Gutter="16">
                        <AntDesign.Col Span="12">
                            <div style="margin-bottom: 16px;">
                                <label style="display: block; margin-bottom: 4px; font-weight: 500;">分组</label>
                                <Input @bind-Value="@option.Group" Placeholder="选项分组（可选）" />
                            </div>
                        </AntDesign.Col>
                        <AntDesign.Col Span="12">
                            <div style="margin-bottom: 16px;">
                                <label style="display: block; margin-bottom: 4px; font-weight: 500;">排序</label>
                                <InputNumber @bind-Value="@option.Order" Min="0" />
                            </div>
                        </AntDesign.Col>
                    </AntDesign.Row>
                    <AntDesign.Row Gutter="16">
                        <AntDesign.Col Span="24">
                            <div style="margin-bottom: 16px;">
                                <label style="display: block; margin-bottom: 4px; font-weight: 500;">描述</label>
                                <TextArea @bind-Value="@option.Description" Rows="2" Placeholder="选项描述（可选）" />
                            </div>
                        </AntDesign.Col>
                    </AntDesign.Row>
                    <Button Type="@ButtonType.Text" Icon="delete" Size="@ButtonSize.Small" OnClick="@(() => RemoveOption(i))" Danger>删除</Button>
                </AntDesign.ChildContent>
            </AntDesign.Card>
        }
    }
    <Button Type="@ButtonType.Dashed" Block OnClick="@AddOption" Icon="plus">添加选项</Button>
</div>

@code {
    [Parameter]
    public List<OptionDataSourceSchema> OptionsList { get; set; }
    [Parameter]
    public EventCallback<List<OptionDataSourceSchema>> OptionsListChanged { get; set; }

    private async Task AddOption()
    {
        OptionsList ??= new List<OptionDataSourceSchema>();
        OptionsList.Add(new OptionDataSourceSchema
        {
            Label = "",
            Value = "",
            IsSelected = false,
            Order = OptionsList.Count,
            Group = "",
            Description = ""
        });
        await OptionsListChanged.InvokeAsync(OptionsList);
        StateHasChanged();
    }

    private async Task RemoveOption(int index)
    {
        if (OptionsList != null && index >= 0 && index < OptionsList.Count)
        {
            OptionsList.RemoveAt(index);
            await OptionsListChanged.InvokeAsync(OptionsList);
            StateHasChanged();
        }
    }
}

<style>
    .fixed-option-datasource-editor .ant-card-small .ant-card-body {
        padding: 12px;
    }
</style>