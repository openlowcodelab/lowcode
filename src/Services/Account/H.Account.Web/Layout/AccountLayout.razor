@namespace H.Account.Web

@inherits DefaultLayoutComponentBase

@code {
    protected override string AppName => "用户管理";

    protected override List<AppMenuItem> MenuItems => new()
    {
        new AppMenuItem { Name = "用户列表", Url = "/account/users", Icon = "👥" }
    };
}
